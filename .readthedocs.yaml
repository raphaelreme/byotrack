version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"
  jobs:
    install:
      # Since the install step is overridden, pip is no longer updated automatically.
      - pip install --upgrade pip
      - pip install .[full] --group doc --group dev

sphinx:
  configuration: docs/source/conf.py
